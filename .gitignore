node_modules
dist
.env
.env.local
.claude/

# Local migration artifacts (contain personal data / local paths — keep out of the public repo)
_dev.log
_migration.sql
_*.py
_*.sql
_backup_*.json
err.log
# Local working material (source docx, transcripts, generated SQL/JSON) — keep out of the public repo
Input/
